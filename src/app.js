import{bulkPrompt,emptyBlueprint,makeZip,n,parseJson,pretty,readiness,setDirectionRequest,slidePrompt,slug}from'./prompting.js';
const $=s=>document.querySelector(s),state={title:'The Journey of a Garment',slides:[{copy:'',cta:'',interaction:'',art:'',role:'Opening'}],bp:null,defaultRef:null,logo:null,bpText:''};Object.assign(state,JSON.parse(localStorage.getItem('ladytin-json-studio')||'{}'));state.bpText=state.bp?pretty(state.bp):pretty(emptyBlueprint(state.title,state.slides.length));
const dl=(name,data,type='application/json')=>{const a=document.createElement('a');a.href=URL.createObjectURL(data instanceof Blob?data:new Blob([data],{type}));a.download=name;a.click();URL.revokeObjectURL(a.href)}, cp=s=>navigator.clipboard?.writeText(s), asset=f=>({id:`asset-${crypto.randomUUID()}`,file:f,filename:f.name,type:f.type||'application/octet-stream'}), save=()=>localStorage.setItem('ladytin-json-studio',JSON.stringify({title:state.title,slides:state.slides.map(({main,reference,...s})=>({...s,main:main&&{id:main.id,filename:main.filename,type:main.type},reference:reference&&{id:reference.id,filename:reference.filename,type:reference.type}})),bp:state.bp}));
function render(){save();const bp=state.bp||emptyBlueprint(state.title,state.slides.length),full=pretty(bulkPrompt(state.slides,bp,state.defaultRef,state.logo)),rows=readiness(state.slides,state.bp,state.defaultRef),ready=rows.every(r=>r.ready);$('#root').innerHTML=`<main class="app"><section class="hero"><div class="brand">LADYTIN</div><h1>Story Studio</h1><p>JSON-only generation packages for individual slides and complete bulk story sets.</p><button id="save">Save project</button></section><section class="card"><h2>01 Story Set</h2><label>Story-set title<input id="title" value="${esc(state.title)}"></label><button id="add">Add slide</button><button class="secondary" id="remove">Remove last slide</button></section><section class="card"><h2>02 Set Direction Package</h2><p class="small">Includes: “Return one valid JSON object only. Do not include markdown, code fences, introductions, explanations or text outside the JSON object.”</p><button id="copyReq">Copy Set Direction Request</button><button class="secondary" id="downReq">Download Request JSON</button><textarea id="bptext">${esc(state.bpText)}</textarea><div class="actions"><button id="valBp">Validate JSON</button><button id="fmtBp" class="secondary">Format JSON</button><button id="impBp">Import Blueprint</button><button id="copyBp" class="secondary">Copy Blueprint JSON</button><button id="downBp" class="secondary">Download Blueprint JSON</button><button id="approve">Approve Set Direction</button><button id="unlock" class="secondary">Unlock Set Direction</button></div><p id="msg"></p></section><section class="card"><h2>03 References & Logos</h2><label>Default set reference<input id="defref" type="file" accept="image/*,application/pdf"></label><p>${state.defaultRef?.filename||''}</p><label>Exact logo file<input id="logo" type="file" accept="image/*"></label><p>${state.logo?.filename||''}</p></section><section class="card"><h2>04 Slides</h2><div class="grid">${state.slides.map((s,i)=>`<div class="card"><h3>Slide ${n(i)}</h3><label>Exact copy<textarea data-i="${i}" data-k="copy">${esc(s.copy||'')}</textarea></label><label>CTA<input data-i="${i}" data-k="cta" value="${esc(s.cta||'')}"></label><label>Interaction<input data-i="${i}" data-k="interaction" value="${esc(s.interaction||'')}"></label><label>Role<input data-i="${i}" data-k="role" value="${esc(s.role||'')}"></label><label>Slide-specific art direction<textarea data-i="${i}" data-k="art">${esc(s.art||'')}</textarea></label><label>Main image/video<input data-file="main" data-i="${i}" type="file" accept="image/*,video/*"></label><p>${s.main?.filename||''}</p><label>Slide reference<input data-file="reference" data-i="${i}" type="file" accept="image/*,application/pdf"></label><p>${s.reference?.filename||'Using default reference when present'}</p><h4>Individual Generation</h4><button data-act="copySlide" data-i="${i}">Copy Slide JSON</button><button class="secondary" data-act="downSlide" data-i="${i}">Download Slide JSON</button><button class="secondary" data-act="zipSlide" data-i="${i}">Download Slide JSON + Attachments</button><button class="secondary" data-act="valSlide" data-i="${i}">Validate Slide JSON</button></div>`).join('')}</div></section><section class="card"><h2>05 Production-ready story set</h2><h2>Bulk Story-Set Generation</h2><div class="row"><b>Slide</b><b>Main asset</b><b>Reference</b><b>JSON valid</b><b>Blueprint / Ready</b></div>${rows.map(r=>`<div class="row"><span>${n(r.slide-1)}</span><span class="${r.main?'ok':'bad'}">${r.main?'Ready':'Missing'}</span><span class="${r.reference?'ok':'bad'}">${r.reference?'Ready':'Missing'}</span><span class="${r.json?'ok':'bad'}">${r.json?'Valid':'Invalid'}</span><span class="${r.ready?'ok':'bad'}">${r.blueprint?'Approved':'Blocked'} / ${r.ready?'Ready':'Blocked: '+r.missing.join(', ')}</span></div>`).join('')}${ready?'':'<p class="bad">This story set is not ready for bulk generation. Resolve the highlighted slide requirements first.</p>'}<button id="copyFull">Copy Full Story-Set JSON</button><button class="secondary" id="downFull">Download Full Story-Set JSON</button><button id="zipFull" ${ready?'':'disabled'}>Download Full Story-Set ZIP</button><button class="secondary" id="valFull">Validate Full Story-Set JSON</button><button class="secondary" id="draftZip">Download Incomplete Draft Package</button><h3>Formatted JSON Preview</h3><button id="toggle" class="secondary">Collapse/Expand</button><button id="regen" class="secondary">Regenerate JSON from current project data</button><p class="ok">${parseJson(full).ok?'Valid JSON':''}</p><pre class="preview">${esc(full)}</pre></section></main>`;wire(bp,full,ready)}
function esc(s){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}function msg(t,ok=true){$('#msg').textContent=t;$('#msg').className=ok?'ok':'bad'}
function wire(bp,full){$('#title').oninput=e=>{state.title=e.target.value;render()};$('#add').onclick=()=>{state.slides.push({copy:'',cta:'',interaction:'',art:'',role:`Slide ${state.slides.length+1}`});render()};$('#remove').onclick=()=>{if(state.slides.length>1)state.slides.pop();render()};$('#save').onclick=save;$('#copyReq').onclick=()=>cp(setDirectionRequest(state.title,state.slides.length));$('#downReq').onclick=()=>dl('set-direction-analysis-request.json',setDirectionRequest(state.title,state.slides.length));$('#bptext').oninput=e=>state.bpText=e.target.value;$('#valBp').onclick=()=>{const r=parseJson(state.bpText);msg(r.ok?'Valid JSON':r.error,r.ok)};$('#fmtBp').onclick=()=>{const r=parseJson(state.bpText);if(r.ok){state.bpText=pretty(r.value);render();msg('Formatted valid JSON')}else msg(r.error,false)};$('#impBp').onclick=()=>{const r=parseJson(state.bpText);if(r.ok){state.bp=r.value;render();msg('Blueprint imported')}else msg(r.error,false)};$('#copyBp').onclick=()=>cp(state.bpText);$('#downBp').onclick=()=>dl('approved-set-style-blueprint.json',state.bpText);$('#approve').onclick=()=>{state.bp={...(state.bp||emptyBlueprint(state.title,state.slides.length)),approval:{status:'approved'}};state.bpText=pretty(state.bp);render()};$('#unlock').onclick=()=>{state.bp={...(state.bp||emptyBlueprint(state.title,state.slides.length)),approval:{status:'draft'}};state.bpText=pretty(state.bp);render()};$('#defref').onchange=e=>{state.defaultRef=asset(e.target.files[0]);render()};$('#logo').onchange=e=>{state.logo=asset(e.target.files[0]);render()};document.querySelectorAll('[data-k]').forEach(el=>el.oninput=e=>{state.slides[+el.dataset.i][el.dataset.k]=e.target.value;save()});document.querySelectorAll('[data-file]').forEach(el=>el.onchange=e=>{state.slides[+el.dataset.i][el.dataset.file]=asset(e.target.files[0]);render()});document.querySelectorAll('[data-act]').forEach(b=>b.onclick=async()=>{const i=+b.dataset.i,s=state.slides[i],json=pretty(slidePrompt(s,i,state.slides,bp,state.defaultRef,state.logo));if(b.dataset.act==='copySlide')cp(json);if(b.dataset.act==='downSlide')dl(`slide-${n(i)}-prompt.json`,json);if(b.dataset.act==='valSlide')alert(parseJson(json).ok?'Slide JSON is valid':'Invalid JSON');if(b.dataset.act==='zipSlide')dl(`slide-${n(i)}-package.zip`,await makeZip([s],{...bp,story_set:{...bp.story_set,slide_count:1}},state.defaultRef,state.logo,true),'application/zip')});$('#copyFull').onclick=()=>cp(full);$('#downFull').onclick=()=>dl('bulk-story-set-prompt.json',full);$('#valFull').onclick=()=>alert(parseJson(full).ok?'Full Story-Set JSON is valid':'Invalid JSON');$('#zipFull').onclick=async()=>dl(`LadyTin-${slug(state.title)}-bulk-generation-package.zip`,await makeZip(state.slides,bp,state.defaultRef,state.logo,false),'application/zip');$('#draftZip').onclick=async()=>dl(`LadyTin-${slug(state.title)}-incomplete-draft-package.zip`,await makeZip(state.slides,bp,state.defaultRef,state.logo,true),'application/zip');$('#toggle').onclick=()=>$('.preview').style.display=$('.preview').style.display==='none'?'block':'none';$('#regen').onclick=render}
render();
