{
  "scripts": {
    "dev": "node server.js",
    "build": "node tests/run-tests.js && rm -rf dist && mkdir -p dist/src && cp index.html dist/ && cp src/*.js src/styles.css dist/src/",
    "lint": "node --check src/app.js && node --check src/prompting.js",
    "test": "node tests/run-tests.js"
  },
  "dependencies": {},
  "devDependencies": {},
  "type": "module"
}
